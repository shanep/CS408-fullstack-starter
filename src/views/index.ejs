<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="/stylesheets/style.css" />
</head>
<body>
  <div class="container py-5">
    <div class="row justify-content-center">
      <div class="col-lg-10">
        <div class="text-center mb-5">
          <p class="text-uppercase text-white-50 mb-2">Welcome (DEBUG DEMO)</p>
          <h1 class="display-4 text-white mb-3">CS408 Simple EC2 Example</h1>
          <p class="lead text-white-50">Two quick examples: a Todo app and an Asteroids game.</p>
        </div>

        <div class="row g-4">
          <div class="col-md-6">
            <div class="card h-100">
              <div class="card-header d-flex align-items-center justify-content-between">
                <div>
                  <h5 class="mb-0">Todo App</h5>
                  <small class="text-muted">SQLite + Express + EJS</small>
                </div>
                <span class="badge bg-primary">Live</span>
              </div>
              <div class="card-body d-flex flex-column">
                <p class="mb-3">Create, list, and delete todos persisted in SQLite.</p>
                <ul class="list-unstyled mb-4">
                  <li>• Total tasks: <strong><%= stats.total %></strong></li>
                  <li>• Completed: <strong><%= stats.completed %></strong></li>
                  <li>• Pending: <strong><%= stats.pending %></strong></li>
                </ul>
                <div class="mt-auto d-flex gap-2">
                  <a href="/todos" class="btn btn-primary w-100">Open Todo App</a>
                </div>
              </div>
            </div>
          </div>

          <div class="col-md-6">
            <div class="card h-100">
              <div class="card-header d-flex align-items-center justify-content-between">
                <div>
                  <h5 class="mb-0">Asteroids Game</h5>
                  <small class="text-muted">Canvas + JS</small>
                </div>
                <span class="badge bg-success">Play</span>
              </div>
              <div class="card-body d-flex flex-column">
                <p class="mb-3">A browser-based homage to the classic arcade game. Use arrows to move, space to shoot, P to pause.</p>
                <div class="mt-auto d-flex gap-2">
                  <a href="/asteroids" class="btn btn-outline-success w-100">Launch Game</a>
                </div>
              </div>
            </div>
          </div>
        </div>

        <div class="text-center text-white-50 mt-5">
          <small>Deploy-ready Express app with SQLite persistence and a sample game view.</small>
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
